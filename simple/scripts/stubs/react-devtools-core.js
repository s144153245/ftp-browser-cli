/**
 * Stub for react-devtools-core. Used in production builds to avoid
 * pulling in the optional dev dependency. Devtools are only used when DEV=true.
 */
export default {
  connectToDevTools() {},
};
